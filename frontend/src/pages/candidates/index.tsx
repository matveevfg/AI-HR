import { Typography, Box } from '@mui/material';

const CandidatesPage = () => {
  return (
    <Box>
      <Typography variant='h4' gutterBottom>
        Кандидаты
      </Typography>
      <Typography>Здесь будет список кандидатов...</Typography>
    </Box>
  );
};

export default CandidatesPage;
