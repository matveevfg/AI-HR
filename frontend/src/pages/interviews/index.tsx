import { Typography, Box } from '@mui/material';

const InterviewsPage = () => {
  return (
    <Box>
      <Typography variant='h4' gutterBottom>
        Интервью
      </Typography>
      <Typography>Здесь будет расписание интервью...</Typography>
    </Box>
  );
};

export default InterviewsPage;
